[project]
name = "gpt4o-tokenizer"
version = "0.1.0"
description = "Inspect tiktoken vocabularies with different language classification strategies."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "hanzidentifier==1.2.0",
    "langdetect==1.0.9",
    "tiktoken==0.12.0",
]
